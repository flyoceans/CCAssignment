Solution05
/*Explain what the following code does: ((n & (n - 1)) == 0)
 ＊If A & B == 0, it means A and B have no common 1s in the same place.
 ＊n should be format like 000010000, and then n-1 equals to 000001111, 
 * So what ((n & (n - 1)) == 0) does is to check if n is a power of 2.
 */
